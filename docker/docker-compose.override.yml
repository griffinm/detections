# Local dev overrides — not committed for prod
services:
  worker-cpu:
    environment:
      VD_DATABASE_URL: postgresql+asyncpg://vd:vd@postgres:5432/video_detection
      VD_REDIS_URL: redis://redis:6379/0

  worker-gpu:
    environment:
      VD_DATABASE_URL: postgresql+asyncpg://vd:vd@postgres:5432/video_detection
      VD_REDIS_URL: redis://redis:6379/0

  ingest-watcher:
    environment:
      VD_REDIS_URL: redis://redis:6379/0
